# Qdrant (required for RAG pipeline)
# Local: http://localhost:6333  |  Qdrant Cloud: https://xxx.aws.cloud.qdrant.io (set QDRANT_API_KEY)
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your-qdrant-api-key

# OpenAI (required for embeddings and generation)
OPENAI_API_KEY=sk-your-openai-api-key

# Weave (optional, for RAG evaluation / tracing)
# WEAVE_PROJECT=your-username/rag-eval
# RAG_EVAL_JUDGE_MODEL=gpt-4o-mini

# RAG backend: qdrant (default) or cognee (requires pip install -e ".[cognee]")
# RAG_BACKEND=qdrant

# Cognee (only when RAG_BACKEND=cognee)
# COGNEE_DATASET_NAME=clin_xplain_data
# VECTOR_DB_PROVIDER=qdrant
# VECTOR_DB_URL=http://localhost:6333
# VECTOR_DATASET_DATABASE_HANDLER=qdrant

# Optional RAG settings
# RAG_INDEX_NAME=rag_docs
# RAG_INDEX_PREFIX=rag_docs
# RAG_EMBEDDING_MODEL=text-embedding-3-small
# RAG_EMBEDDING_DIMENSIONS=1536
# RAG_CHUNK_SIZE=1000
# RAG_CHUNK_OVERLAP=200
# RAG_TOP_K=5
# RAG_LLM_MODEL=gpt-4o-mini
# RAG_LLM_TEMPERATURE=0.0
# RAG_CACHE_TTL=3600  # LLM cache TTL (seconds); 0 = disable
