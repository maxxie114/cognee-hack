# ── Qdrant Cloud ──
# Get these from https://cloud.qdrant.io/
QDRANT_URL=https://your-cluster-id.us-east4-0.gcp.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key

# ── LLM / Embedding (set by docker-compose, override here if needed) ──
# LLM_API_KEY=.
# LLM_PROVIDER=ollama
# LLM_MODEL=cognee-distillabs-model-gguf-quantized
# LLM_ENDPOINT=http://ollama:11434/v1
# LLM_MAX_TOKENS=16384
# EMBEDDING_PROVIDER=ollama
# EMBEDDING_MODEL=nomic-embed-text:latest
# EMBEDDING_ENDPOINT=http://ollama:11434/api/embed
# EMBEDDING_DIMENSIONS=768
# HUGGINGFACE_TOKENIZER=nomic-ai/nomic-embed-text-v1.5
